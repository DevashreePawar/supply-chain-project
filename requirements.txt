# ============================================================================
# Python dependencies for the dbt + Streamlit pipeline.
#
# Install locally:
#     python -m venv .venv
#     .venv/bin/pip install -r requirements.txt
#
# Used by GitHub Actions for the CI + Docs workflows.
# ============================================================================

# --- dbt (data transformation layer) ---
dbt-core>=1.11,<1.12
dbt-snowflake>=1.11,<1.12

# --- Environment + secrets loading ---
python-dotenv>=1.0

# --- Snowflake direct connection (used by Streamlit app + ad-hoc scripts) ---
snowflake-connector-python>=3.0

# --- Streamlit app (Phase 3) ---
streamlit>=1.40
pandas>=2.0
altair>=5.0

# --- Forecasting package (Phase 5) ---
prophet>=1.1
statsmodels>=0.14
scipy>=1.11
scikit-learn>=1.3

# --- OpenAI (added in Session 6 for NL → SQL) ---
openai>=1.50
tiktoken>=0.8
